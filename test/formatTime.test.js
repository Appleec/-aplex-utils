/**
 *  Create by appleex on 2022/4/13 9:52 上午.
 */
import { formatTime } from './../lib/time'

describe('formatTime', () => {
    it('formatTime...', () => {
        console.log(formatTime(new Date('2022-1-1'), 'YYYY'));
        // expect(byteLength('abcd')).toEqual(4);
    });
});
