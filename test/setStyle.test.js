/**
 *  Create by appleex on 2022/4/13 9:52 上午.
 */
import setStyle from './../lib/setStyle'

describe('setStyle', () => {
    it('setStyle...', () => {
        document.body.innerHTML = '<div id="wrapper" style="text-align: center;"></div>';
        // var _wrapper = document.querySelector('#wrapper');

        // setStyle(_wrapper, 'color', 'red');
        // setStyle(_wrapper, { color: 'red', fontSize: '13px' }, 'red');

        // console.log(JSON.stringify(_wrapper.style));
    });
});
